'use babel';

import LanguageFormalityCoreView from '../lib/language-formality-core-view';

describe('LanguageFormalityCoreView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
